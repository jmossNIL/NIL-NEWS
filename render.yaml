# ===== BEGIN render.yaml =====
services:
  - type: web
    name: nil-news            # you can change this label if you like
    env: docker
    plan: free                # Render free tier is sufficient
    dockerfilePath: Dockerfile
    envVars:
      - key: OPENAI_API_KEY   # optional — leave blank for excerpt fallback
        sync: false           # keeps the key out of your repo history
# ===== END render.yaml =====
